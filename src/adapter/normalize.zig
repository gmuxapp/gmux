// Package adapter normalizes commands.
const std = @import("std");

/// normalize normalizes a command for consistent matching.
pub fn normalize(command: []const u8) []const u8 {
    _ = command;
    return "";
}
