---
import heroDesktop from '../assets/hero-desktop.png'
import heroMobile from '../assets/hero-mobile.png'
import PostHog from '../components/posthog.astro'
import Topology from '../components/Topology.astro'
---

<html lang="en">
<head>
  <meta charset="utf-8" />
  <PostHog />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>gmux — a session manager for AI coding agents</title>
  <meta name="description" content="Prefix any command with gmux and it shows up in a live browser dashboard. See what every agent is doing at a glance, from your desktop or your phone." />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Sans:wght@400;500;600;700&family=Source+Sans+3:ital,wght@0,400;0,500;0,600;1,400&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
</head>
<body>
  <!-- Nav -->
  <nav class="nav">
    <div class="nav-inner">
      <a href="/" class="nav-logo">gmux</a>
      <div class="nav-links">
        <a href="/getting-started/">Docs</a>
        <a href="https://discord.gg/Mg6EJHFZxu" class="nav-icon" aria-label="Discord">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M20.317 4.37a19.791 19.791 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028c.462-.63.874-1.295 1.226-1.994a.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03z"/></svg>
        </a>
        <a href="https://github.com/gmuxapp/gmux" class="nav-icon" aria-label="GitHub">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12z"/></svg>
        </a>
      </div>
    </div>
  </nav>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-text">
        <h1>Steer every agent from any device</h1>
        <p class="hero-sub">
          Prefix any command with <code>gmux</code> to open the same live session
          in your terminal and any number of browsers.
        </p>
        <div class="hero-install">
          <div class="install-box">
            <div class="install-tabs" id="install-tabs">
              <button class="tab-btn active" data-os="mac">macOS</button>
              <button class="tab-btn" data-os="linux">Linux</button>
            </div>
            <div class="install-cmd" id="install-cmd">
              <code data-cmd-mac="brew install gmuxapp/tap/gmux" data-cmd-linux="curl -sSfL https://gmux.app/install.sh | sh">brew install gmuxapp/tap/gmux</code>
              <button class="copy-btn" id="copy-btn" aria-label="Copy to clipboard">
                <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
                <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="display:none"><polyline points="20 6 9 17 4 12"/></svg>
              </button>
            </div>
          </div>
          <a class="script-link" href="/install.sh">What does this script do?</a>
        </div>
        <div class="hero-actions">
          <a href="/getting-started/" class="hero-cta">Get started</a>
          <a href="https://github.com/gmuxapp/gmux" class="hero-secondary">Source on GitHub</a>
        </div>
      </div>
      <div class="hero-visual" aria-hidden="true">
        <div class="hero-stage">
          <img
            class="hero-desktop-shot"
            src={heroDesktop.src}
            alt=""
            width={heroDesktop.width}
            height={heroDesktop.height}
            loading="eager"
          />
          <img
            class="hero-mobile-shot"
            src={heroMobile.src}
            alt=""
            width={heroMobile.width}
            height={heroMobile.height}
            loading="eager"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Topology -->
  <section class="section">
    <div class="section-inner">
      <h2 class="topo-heading">Every session, from every machine, in every&nbsp;browser</h2>
      <Topology />
    </div>
  </section>

  <!-- Networking (Tailscale) -->
  <section class="section section-alt">
    <div class="section-inner two-col">
      <div class="two-col-text">
        <h2>Tailscale handles the network</h2>
        <p class="section-lead">Run <code>gmuxd remote</code> once to enable the built-in Tailscale listener. No open ports to manage, no auth to configure. Only your devices can reach your sessions, over encrypted tunnels.</p>
      </div>
      <div class="two-col-visual">
        <pre class="code-snippet"><code><span class="code-comment"># one-time setup</span>
gmuxd remote

<span class="code-comment"># any time after</span>
gmux &lt;command&gt;</code></pre>
      </div>
    </div>
  </section>

  <!-- Terminal compat + persistence -->
  <section class="section">
    <div class="section-inner two-col two-col-reverse">
      <div class="two-col-text">
        <h2>Sessions outlive your terminal</h2>
        <p class="section-lead">Add <code>gmux</code> to your shell aliases and start a session from any terminal. Close the terminal whenever you want; the session keeps running headless, available from any browser.</p>
      </div>
      <div class="two-col-visual">
        <pre class="code-snippet"><code>alias claude = "gmux claude"
alias codex  = "gmux codex"
alias pi     = "gmux pi"</code></pre>
      </div>
    </div>
  </section>

  <!-- Adapters + notifications -->
  <section class="section section-alt">
    <div class="section-inner two-col">
      <div class="two-col-text">
        <h2>Know exactly when to check in</h2>
        <p class="section-lead">Adapters read live state from each agent (codex, Claude, pi, shell) and surface it as status dots. When an agent finishes or needs input, gmux fires a notification on whichever device you're using.</p>
      </div>
      <div class="two-col-visual">
        <div class="mock-notif">
          <div class="mock-notif-header">
            <span class="mock-notif-app">
              <svg class="mock-notif-icon" viewBox="0 0 32 32" width="18" height="18" aria-hidden="true">
                <rect width="32" height="32" rx="6" fill="#0f141a"/>
                <rect x="3.5" y="3.5" width="25" height="25" rx="3.5" fill="none" stroke="#49b8b8" stroke-width="1.5"/>
                <polyline points="8,10 16,16 8,22" fill="none" stroke="#49b8b8" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/>
                <line x1="18" y1="22" x2="25" y2="22" stroke="#49b8b8" stroke-width="2.4" stroke-linecap="round"/>
              </svg>
              <span>gmux</span>
            </span>
            <span class="mock-notif-time">now</span>
          </div>
          <div class="mock-notif-title">codex · refactor adapters</div>
          <div class="mock-notif-body">Finished. 4 files changed.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Closing CTA -->
  <section class="section cta-section">
    <div class="section-inner cta-inner">
      <h2>Try it in two&nbsp;minutes</h2>
      <div class="cta-install">
        <div class="install-box">
          <div class="install-tabs" id="install-tabs-bottom">
            <button class="tab-btn active" data-os="mac">macOS</button>
            <button class="tab-btn" data-os="linux">Linux</button>
          </div>
          <div class="install-cmd" id="install-cmd-bottom">
            <code data-cmd-mac="brew install gmuxapp/tap/gmux" data-cmd-linux="curl -sSfL https://gmux.app/install.sh | sh">brew install gmuxapp/tap/gmux</code>
            <button class="copy-btn" id="copy-btn-bottom" aria-label="Copy to clipboard">
              <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
              <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="display:none"><polyline points="20 6 9 17 4 12"/></svg>
            </button>
          </div>
        </div>
        <a class="script-link" href="/install.sh">What does this script do?</a>
      </div>
      <a href="/getting-started/" class="hero-cta">Get started</a>
      <p class="cta-discord">Have questions? <a href="https://discord.gg/Mg6EJHFZxu">Join the Discord</a></p>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-inner">
      <span class="footer-logo">gmux</span>
      <div class="footer-links">
        <a href="/getting-started/">Docs</a>
        <a href="https://github.com/gmuxapp/gmux">GitHub</a>
        <a href="https://discord.gg/Mg6EJHFZxu">Discord</a>
        <span class="footer-sep">&middot;</span>
        <span class="footer-license">MIT License</span>
      </div>
      <div class="footer-socials">
        <a href="https://bsky.app/profile/mgabor.hu" aria-label="Bluesky" target="_blank" rel="noopener">
          <svg width="18" height="18" viewBox="0 0 568 501" fill="currentColor"><path d="M123.121 33.664C188.241 82.553 258.281 181.68 284 234.873c25.719-53.192 95.759-152.32 160.879-201.21C491.866-1.611 568-28.906 568 57.947c0 17.346-9.945 145.713-15.778 166.555-20.275 72.453-94.155 90.933-159.875 79.748C507.222 323.8 536.444 388.56 473.333 453.32c-119.86 122.992-172.272-30.859-185.702-70.281-2.462-7.227-3.614-10.608-3.631-7.733-.017-2.875-1.169.506-3.631 7.733-13.43 39.422-65.842 193.273-185.702 70.281-63.111-64.76-33.889-129.52 80.986-149.071-65.72 11.185-139.6-7.295-159.875-79.748C9.945 203.659 0 75.291 0 57.946 0-28.906 76.135-1.612 123.121 33.664Z"/></svg>
        </a>
        <a href="https://x.com/mgabor_" aria-label="X" target="_blank" rel="noopener">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
        </a>
        <a href="https://ko-fi.com/mgabor" aria-label="Ko-fi" target="_blank" rel="noopener">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M23.881 8.948c-.773-4.085-4.859-4.593-4.859-4.593H.723c-.604 0-.679.798-.679.798s-.082 7.324-.022 11.822c.164 2.424 2.586 2.672 2.586 2.672s8.267-.023 11.966-.049c2.438-.426 2.683-2.566 2.658-3.734 4.352.24 7.422-2.831 6.649-6.916zm-11.062 3.511c-1.246 1.453-4.011 3.976-4.011 3.976s-.121.119-.31.023c-.076-.057-.108-.09-.108-.09-.443-.441-3.368-3.049-4.034-3.954-.709-.965-1.041-2.7-.091-3.71.951-1.01 3.005-1.086 4.363.407 0 0 1.565-1.782 3.468-.963 1.904.82 1.832 3.011.723 4.311zm6.173.478c-.928.116-1.682.028-1.682.028V7.284h1.77s1.971.551 1.971 2.638c0 1.913-.985 2.667-2.059 3.015z"/></svg>
        </a>
      </div>
    </div>
  </footer>

  <script>
    const commands = {
      mac: 'brew install gmuxapp/tap/gmux',
      linux: 'curl -sSfL https://gmux.app/install.sh | sh',
    }

    // Detect OS from user agent
    function detectOS() {
      const ua = navigator.userAgent || navigator.platform || ''
      if (/Linux/.test(ua)) return 'linux'
      return 'mac'
    }

    // Switch all tab groups to a given OS
    function switchOS(os) {
      document.querySelectorAll('.install-tabs').forEach(tabs => {
        tabs.querySelectorAll('.tab-btn').forEach(btn => {
          btn.classList.toggle('active', btn.dataset.os === os)
        })
      })
      document.querySelectorAll('.install-cmd code[data-cmd-mac]').forEach(code => {
        code.textContent = commands[os]
      })
      document.querySelectorAll('.script-link').forEach(link => {
        link.style.visibility = os === 'linux' ? 'visible' : 'hidden'
      })
    }

    // Reorder tabs so the detected OS comes first
    function reorderTabs(os) {
      document.querySelectorAll('.install-tabs').forEach(tabs => {
        const btns = [...tabs.querySelectorAll('.tab-btn')]
        const first = btns.find(b => b.dataset.os === os)
        if (first && first !== btns[0]) {
          tabs.insertBefore(first, btns[0])
        }
      })
    }

    // Wire up tab clicks (synced across both tab groups)
    document.querySelectorAll('.install-tabs .tab-btn').forEach(btn => {
      btn.addEventListener('click', () => switchOS(btn.dataset.os))
    })

    // Copy button
    function setupCopyBtn(id) {
      const btn = document.getElementById(id)
      if (!btn) return
      btn.addEventListener('click', () => {
        const code = btn.closest('.install-cmd').querySelector('code')
        navigator.clipboard.writeText(code.textContent)
        const copy = btn.querySelector('.copy-icon')
        const check = btn.querySelector('.check-icon')
        if (copy && check) {
          copy.style.display = 'none'
          check.style.display = 'block'
          setTimeout(() => {
            copy.style.display = 'block'
            check.style.display = 'none'
          }, 2000)
        }
      })
    }
    setupCopyBtn('copy-btn')
    setupCopyBtn('copy-btn-bottom')

    // Auto-detect on load
    const detected = detectOS()
    reorderTabs(detected)
    switchOS(detected)

    // Reveal after OS detection to avoid flash of wrong command
    document.querySelectorAll('.install-box').forEach(box => {
      box.classList.add('ready')
    })
  </script>
</body>
</html>

<style>
  :where(*, *::before, *::after) {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --bg: oklch(12% 0.015 250);
    --bg-alt: oklch(15% 0.015 250);
    --text: oklch(88% 0.01 250);
    --text-secondary: oklch(65% 0.01 250);
    --text-muted: oklch(48% 0.01 250);
    --accent: oklch(72% 0.1 195);
    --border: oklch(22% 0.015 250);
    --border-subtle: oklch(18% 0.01 250);
    --code-bg: oklch(16% 0.015 250);
    --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
  }

  html {
    font-size: 16px;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body {
    font-family: 'Source Sans 3', 'Segoe UI', system-ui, sans-serif;
    color: var(--text);
    background: var(--bg);
    line-height: 1.6;
  }

  h1, h2, h3 {
    font-family: 'Instrument Sans', sans-serif;
    line-height: 1.15;
    letter-spacing: -0.025em;
  }

  a { color: var(--accent); text-decoration: none; }
  a:hover { text-decoration: underline; }

  code {
    font-family: 'Fira Code', 'JetBrains Mono', monospace;
    font-size: 0.85em;
    background: var(--code-bg);
    padding: 0.15em 0.4em;
    border-radius: 4px;
  }

  .brand {
    font-family: 'Instrument Sans', sans-serif;
    font-weight: 700;
    letter-spacing: -0.04em;
    color: var(--text);
  }

  /* --- Nav --- */
  .nav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: oklch(12% 0.015 250 / 0.85);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border-subtle);
  }

  .nav-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 4vw, 2.5rem);
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-logo {
    font-family: 'Instrument Sans', sans-serif;
    font-weight: 700;
    font-size: 20px;
    color: var(--text);
    letter-spacing: -0.04em;
  }
  .nav-logo:hover { text-decoration: none; }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }

  .nav-links a {
    color: var(--text-secondary);
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.15s;
  }
  .nav-links a:hover { color: var(--text); text-decoration: none; }

  .nav-icon { display: flex; align-items: center; color: var(--text-muted) !important; }
  .nav-icon:hover { color: var(--text) !important; }

  /* --- Hero --- */
  .hero {
    padding: clamp(3rem, 7vw, 5.5rem) 0 clamp(2rem, 5vw, 3.5rem);
  }

  .hero-content {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 4vw, 2.5rem);
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.2fr);
    gap: clamp(2rem, 3.5vw, 3.5rem);
    align-items: center;
  }

  .hero-text {
    max-width: 36rem;
  }

  .hero h1 {
    font-size: clamp(2.1rem, 4vw, 3.4rem);
    font-weight: 700;
    color: var(--text);
    margin-bottom: 1rem;
    letter-spacing: -0.04em;
    line-height: 1.05;
    text-wrap: balance;
  }

  .hero-sub {
    font-size: clamp(1rem, 1.15vw, 1.12rem);
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 1.5rem;
    max-width: 40ch;
  }

  .hero-install {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    margin-bottom: 1rem;
  }

  .install-box {
    display: inline-flex;
    flex-direction: column;
    border: 1px solid var(--border);
    border-radius: 8px;
    overflow: hidden;
    background: var(--code-bg);
    opacity: 0;
  }

  .install-box.ready {
    opacity: 1;
    transition: opacity 0.15s;
  }

  .install-tabs {
    display: flex;
    border-bottom: 1px solid var(--border);
  }

  .tab-btn {
    font-family: 'Instrument Sans', sans-serif;
    font-size: 0.78rem;
    font-weight: 500;
    padding: 0.35rem 0.85rem;
    border: none;
    background: transparent;
    color: var(--text-muted);
    cursor: pointer;
    position: relative;
    transition: color 0.15s;
  }

  .tab-btn.active {
    color: var(--text);
    background: oklch(18% 0.015 250);
  }

  .tab-btn.active::after {
    content: '';
    position: absolute;
    bottom: -1px;
    left: 0;
    right: 0;
    height: 1px;
    background: oklch(18% 0.015 250);
  }

  .tab-btn:hover:not(.active) {
    color: var(--text-secondary);
  }

  .script-link {
    visibility: hidden;
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-top: 0.4rem;
  }

  .script-link:hover {
    color: var(--text-secondary);
  }

  .install-cmd {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: oklch(18% 0.015 250);
    padding: 0.55rem 0.65rem 0.55rem 0.85rem;
    font-family: 'Fira Code', 'JetBrains Mono', monospace;
    font-size: 0.85rem;
    color: var(--text);
  }

  .install-cmd code {
    font-size: inherit;
    background: none;
    padding: 0;
    border-radius: 0;
  }

  .copy-btn {
    background: none;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 4px;
    border-radius: 4px;
    display: flex;
    align-items: center;
    transition: color 0.15s;
  }
  .copy-btn:hover { color: var(--text); }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 1.1rem;
    flex-wrap: wrap;
  }

  .hero-cta {
    position: relative;
    font-family: 'Instrument Sans', sans-serif;
    font-weight: 600;
    font-size: 0.94rem;
    color: var(--bg);
    background: var(--text);
    padding: 0.62rem 1.1rem;
    border-radius: 8px;
    overflow: hidden;
    isolation: isolate;
    box-shadow: 0 1px 3px oklch(4% 0.01 250 / 0.3);
    transition:
      color 0.35s var(--ease-out-expo),
      box-shadow 0.5s var(--ease-out-expo),
      transform 0.5s var(--ease-out-expo);
  }
  /* Layer 1: teal gradient fill, wipes in from left */
  .hero-cta::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, oklch(52% 0.14 190), oklch(42% 0.16 225));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s var(--ease-out-expo);
    z-index: -1;
  }
  /* Layer 2: bright streak sweeps across, delayed */
  .hero-cta::after {
    content: '';
    position: absolute;
    top: 0;
    left: -130%;
    width: 80%;
    height: 100%;
    background: linear-gradient(
      105deg,
      transparent 20%,
      oklch(100% 0 200 / 0.18) 38%,
      oklch(100% 0 200 / 0.45) 50%,
      oklch(100% 0 200 / 0.18) 62%,
      transparent 80%
    );
    z-index: 1;
    pointer-events: none;
    transition: none;
  }
  .hero-cta:hover {
    color: oklch(97% 0.02 195);
    text-decoration: none;
    transform: translateY(-1px);
    box-shadow:
      0 0 20px oklch(52% 0.14 195 / 0.4),
      0 0 50px oklch(48% 0.12 210 / 0.18),
      0 8px 20px oklch(4% 0.01 250 / 0.35);
  }
  .hero-cta:hover::before {
    transform: scaleX(1);
  }
  .hero-cta:hover::after {
    left: 160%;
    transition: left 0.7s 0.22s var(--ease-out-expo);
  }

  .hero-secondary {
    font-size: 0.94rem;
    font-weight: 500;
    color: var(--text-secondary);
  }
  .hero-secondary:hover { color: var(--text); text-decoration: none; }

  /* Hero visual — fixed-ratio composition */
  .hero-visual {
    min-width: 0;
  }

  .hero-stage {
    position: relative;
    width: 100%;
    max-width: 820px;
    margin-left: auto;
    /* bottom padding reserves space for the phone overhang */
    padding-bottom: 4%;
  }

  .hero-stage::before {
    content: '';
    position: absolute;
    inset: 10% 10% 14% 12%;
    background: radial-gradient(
      circle at 45% 40%,
      oklch(16% 0.018 250 / 0.6),
      transparent 70%
    );
    filter: blur(28px);
    pointer-events: none;
  }

  .hero-desktop-shot,
  .hero-mobile-shot {
    display: block;
    height: auto;
    position: relative;
  }

  .hero-desktop-shot {
    width: 100%;
    border-radius: 4px;
    border: 1px solid oklch(100% 0 0 / 0.08);
    box-shadow: 0 24px 60px oklch(4% 0.01 250 / 0.4);
  }

  /* Phone: sized as a fixed percentage of desktop so they scale together */
  .hero-mobile-shot {
    position: absolute;
    width: 35%;
    right: -1rem;
    bottom: 0;
    border-radius: 16px;
    border: clamp(4px, 1.2vw, 10px) solid oklch(20% 0.01 250);
    box-shadow: 0 18px 50px oklch(4% 0.01 250 / 0.75);
  }

  /* --- Sections --- */
  .section {
    padding: clamp(3rem, 6vw, 5rem) 0;
  }

  .section-alt {
    background: var(--bg-alt);
  }

  .section-inner {
    max-width: 1040px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 4vw, 2.5rem);
  }

  .section h2 {
    font-size: clamp(1.5rem, 2.5vw, 2rem);
    font-weight: 700;
    margin-bottom: 0.5rem;
  }

  .section-lead {
    color: var(--text-secondary);
    font-size: 1.05rem;
    margin-bottom: 2rem;
    max-width: 50ch;
  }

  .topo-heading {
    text-align: center;
    margin-bottom: 0.5rem;
  }

  /* Two-column content sections */
  .two-col {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 4vw, 3rem);
    align-items: center;
  }

  .two-col-reverse .two-col-text { order: 2; }
  .two-col-reverse .two-col-visual { order: 1; }

  .two-col-text h2 {
    margin-bottom: 0.75rem;
  }

  .two-col-text .section-lead {
    margin-bottom: 0;
    max-width: 46ch;
  }

  .two-col-visual {
    display: flex;
    justify-content: flex-end;
  }

  .two-col-reverse .two-col-visual {
    justify-content: flex-start;
  }

  /* Code snippet blocks */
  .code-snippet {
    background: var(--code-bg);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 1.15rem 1.4rem;
    font-family: 'Fira Code', 'JetBrains Mono', monospace;
    font-size: 0.875rem;
    line-height: 1.7;
    color: var(--text);
    margin: 0;
    overflow-x: auto;
    max-width: 100%;
    min-width: 290px;
  }

  .code-snippet code {
    background: none;
    padding: 0;
    font-size: inherit;
    white-space: pre;
  }

  .code-comment {
    color: var(--text-muted);
  }

  /* Mock notification */
  .mock-notif {
    background: oklch(22% 0.012 250 / 0.92);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid oklch(100% 0 0 / 0.08);
    border-radius: 14px;
    padding: 12px 14px;
    box-shadow: 0 20px 50px oklch(4% 0.01 250 / 0.5);
    width: 100%;
    max-width: 340px;
    font-family: 'Source Sans 3', sans-serif;
  }

  .mock-notif-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.72rem;
    color: var(--text-muted);
    margin-bottom: 5px;
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .mock-notif-app {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-weight: 600;
    color: var(--text-secondary);
  }

  .mock-notif-icon {
    border-radius: 4px;
    flex-shrink: 0;
  }

  .mock-notif-time {
    font-weight: 500;
  }

  .mock-notif-title {
    font-size: 0.92rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 2px;
    line-height: 1.3;
  }

  .mock-notif-body {
    font-size: 0.88rem;
    color: var(--text-secondary);
    line-height: 1.45;
  }

  /* --- Closing CTA --- */
  .cta-inner {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
  }

  .cta-section h2 {
    margin-bottom: 0;
  }

  .cta-install {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
  }

  .cta-discord {
    font-size: 0.95rem;
    color: var(--text-secondary);
    margin-top: 0.25rem;
  }

  .cta-discord a {
    color: var(--text);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .cta-discord a:hover {
    color: white;
  }

  /* --- Footer --- */
  .footer {
    padding: 2rem 0;
    border-top: 1px solid var(--border-subtle);
  }

  .footer-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 4vw, 2.5rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .footer-logo {
    font-family: 'Instrument Sans', sans-serif;
    font-weight: 700;
    font-size: 1rem;
    color: var(--text-muted);
    letter-spacing: -0.04em;
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    font-size: 0.85rem;
  }
  .footer-links a { color: var(--text-secondary); }
  .footer-sep { color: var(--text-muted); }
  .footer-license { color: var(--text-muted); }

  .footer-socials {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .footer-socials a {
    color: var(--text-muted);
    transition: color 0.15s;
    display: flex;
  }

  .footer-socials a:hover {
    color: var(--text);
    text-decoration: none;
  }

  /* --- Responsive --- */
  @media (max-width: 900px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .hero-text { text-align: center; min-width: 0; }
    .hero h1 { font-size: 2.4rem; }
    .hero-sub { max-width: 32ch; margin-inline: auto; }
    .hero-install { align-items: center; width: 100%; }
    .install-box { max-width: 100%; }
    .hero-actions { justify-content: center; }
    .hero-stage { max-width: 100%; }

    .two-col { grid-template-columns: 1fr; gap: 1.5rem; }
    .two-col-reverse .two-col-text { order: 1; }
    .two-col-reverse .two-col-visual { order: 2; }
    .two-col-text { text-align: center; }
    .two-col-text .section-lead { max-width: 38ch; margin-inline: auto; }
    .two-col-visual { justify-content: center; }
    .two-col-reverse .two-col-visual { justify-content: center; }
  }

  @media (max-width: 640px) {
    .hero { padding-top: 2.4rem; }
    .hero h1 { font-size: 2rem; }
    .hero-sub { font-size: 1rem; }

    .install-box {
      display: flex;
      width: 100%;
    }

    .install-cmd {
      justify-content: space-between;
    }

    .install-cmd code {
      white-space: nowrap;
      overflow-x: auto;
      scrollbar-width: none;
      flex: 1;
      min-width: 0;
    }
    .install-cmd code::-webkit-scrollbar { display: none; }

    .cta-install { align-self: stretch; }

    .footer-links { font-size: 0.8rem; gap: 0.6rem; }
  }

  /* --- Animations --- */
  @media (prefers-reduced-motion: no-preference) {
    .hero-text { animation: fadeUp 0.5s var(--ease-out-expo) both; }
    .hero-visual { animation: fadeUp 0.6s 0.1s var(--ease-out-expo) both; }
    .section { animation: fadeUp 0.5s var(--ease-out-expo) both; }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(12px); }
      to { opacity: 1; transform: translateY(0); }
    }
  }
</style>
