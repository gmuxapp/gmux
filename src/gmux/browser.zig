// Package gmux provides the browser command.
const std = @import("std");

/// openBrowser opens the browser to the gmux UI.
pub fn openBrowser(url: []const u8) !void {
    _ = url;
    // In real implementation, this would open the browser
}
